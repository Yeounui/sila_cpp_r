// Generated by sila2-codegen — do not edit.
#pragma once

#include <string_view>
{% if provisioned_schemas %}
#include <span>

#include <sila/server/CommandParameterValidator.h>
{% endif %}

namespace sila2::generated::{{ namespace_lower }} {

// Fully Qualified Identifier
inline constexpr std::string_view kFqi =
    "{{ fqi }}";

// Feature version
inline constexpr std::string_view kFeatureVersion = "{{ feature_version }}";

// FDL XML original (defined in {{ feature_identifier }}Meta.cc)
extern const std::string_view kFdlXml;
{% if provisioned_schemas %}

// R10-9g1: content for every Schema constraint whose FDL source is a Url,
// fetched/embedded at codegen time (Part A p70: URL is an alternative source
// of the schema the value MUST comply with, not an exemption from A224).
// Defined in {{ feature_identifier }}Meta.cc.
extern const std::span<const sila2::ProvisionedSchema> kProvisionedSchemas;
{% endif %}
{% for error in errors %}

inline constexpr std::string_view kError_{{ error.identifier }} =
    "{{ fqi }}/DefinedExecutionError/{{ error.identifier }}";
{% endfor %}

}  // namespace sila2::generated::{{ namespace_lower }}
