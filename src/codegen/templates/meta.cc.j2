// Generated by sila2-codegen — do not edit.
#include "{{ feature_identifier }}Meta.h"

namespace sila2::generated::{{ namespace_lower }} {

// The XML must start right after the raw-string delimiter: an XML declaration
// preceded by a newline is not well-formed, and XSD-validating clients
// (sila2-python) reject the served FeatureDefinition outright.
const std::string_view kFdlXml = R"{{ raw_delimiter }}({{ xml_text }}
){{ raw_delimiter }}";
{% if provisioned_schemas %}

// R10-9g1: fetched/embedded at codegen time from each distinct Schema Url
// (Part A p70); the served kFdlXml above keeps the original <Url> untouched.
static constexpr sila2::ProvisionedSchema kProvisionedSchemasStorage[] = {
{% for schema in provisioned_schemas %}
    {"{{ schema.url }}", R"{{ schema.raw_delimiter }}({{ schema.schema_xml }}){{ schema.raw_delimiter }}"},
{% endfor %}
};
const std::span<const sila2::ProvisionedSchema> kProvisionedSchemas{kProvisionedSchemasStorage};
{% endif %}

}  // namespace sila2::generated::{{ namespace_lower }}
