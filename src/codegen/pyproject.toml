[project]
name = "sila2-codegen"
version = "0.81.0"
requires-python = ">=3.10"
dependencies = [
    "lxml",
    "xsdata",
    "Jinja2",
]

[dependency-groups]
dev = ["pytest"]
validation = [
    "pytest>=7.0",
    "sila2>=0.3.0",
]

[tool.pytest.ini_options]
testpaths = ["../../tests/codegen"]
